FROM node:20-slim

ENV DOT_MCP_HOST=0.0.0.0 \
    DOT_MCP_PORT=8000

WORKDIR /app

COPY package.json package-lock.json tsconfig.json README.md ./
COPY src ./src

RUN npm ci && npm run build && npm prune --omit=dev

EXPOSE 8000

CMD ["npm", "start"]

